INJECT:law_no_colonial_affairs = {

	can_enact = {
		custom_tooltip = {
			text = no_unincorp_states_tt
			NOT = {
				any_state = {
					is_incorporated = no
					owner = ROOT
				}
			}
		}
	}
	#modifier = {
	#	country_acceptance_homeland_add = 10
	#}
}

REPLACE:law_frontier_colonization = { #REPLACE BECAUSE INSTITUTION MUST BE REMOVED
	group = lawgroup_colonization

	can_enact = {
		OR = {
			AND = {
				country_is_colonial_or_company = yes
				any_decentralized_country = {
					is_adjacent_to_country = root
				}
			}
			any_scope_state = {
				is_homeland_of_country_cultures = ROOT
				any_neighbouring_state = {
					owner = {
						is_country_type = decentralized
					}
				}
			}
		}
	}

	icon = "gfx/interface/icons/law_icons/frontier_colonization.dds"

	progressiveness = 0

	disallowing_laws = {
		law_sakoku
	}

	unlocking_technologies = {
		colonization
	}

	modifier = {
		state_migration_pull_unincorporated_mult = 0.5
		state_colony_growth_creation_factor = 0.2
	}


	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_colonization_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_will_do = {
		NOR = {
			has_law_or_variant = law_type:law_colonial_exploitation
			has_law_or_variant = law_type:law_colonial_resettlement
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
		
		if = {
			limit = { 
				has_journal_entry = je_taming_the_north
			}
			add = 250
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				OR = {
					has_law_or_variant = law_type:law_colonial_resettlement
					has_law = law_type:law_frontier_colonization
				}
				scope:target_country = {
					has_law_or_variant = law_type:law_no_colonial_affairs
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_colonial_offices }
			}
			add = 10
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}

INJECT:law_colonial_resettlement = {
	modifier = {
		#state_migration_pull_unincorporated_mult = 0.5 #VANILLA MODIFIER
		
		state_unincorporated_starting_wages_mult = 0.10
		building_unincorporated_subsistence_output_mult = 0.10
		building_group_bg_manufacturing_unincorporated_throughput_add = 0.1
		building_group_bg_mining_unincorporated_throughput_add = 0.1
	}

	institution = institution_colonial_affairs

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_colonization_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_will_do = {
		OR = {
			has_law_or_variant = law_type:law_colonial_exploitation
			navy_size >= 50
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
		
		if = {
			limit = { 
				has_journal_entry = je_taming_the_north
			}
			add = 500
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				OR = {
					has_law = law_type:law_colonial_resettlement
					has_law_or_variant = law_type:law_frontier_colonization
				}
				scope:target_country = {
					has_law_or_variant = law_type:law_no_colonial_affairs
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_colonial_offices }
			}
			add = 10
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}

-COLONIAL EXPLOITATION UNCHANGED