INJECT:grant_own_market = {
	pact = {
		actor_can_break = {
			scope:target_country ?= {
				NOT = {
					has_variable = HMPS_is_forced_own_market
				}
			}
		}
		
		target_can_break = {
			NOT = {
				has_variable = HMPS_is_forced_own_market
			}
		}
	}
}

