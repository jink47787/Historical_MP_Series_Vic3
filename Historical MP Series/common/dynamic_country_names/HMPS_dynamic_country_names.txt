INJECT:DEFAULT = { #Results in names like "French India" for a colonial subject in india (maybe wanna do company later)
	dynamic_country_name = {
		name = BHT
		adjective = BHT_ADJ
		use_overlord_prefix = yes
		
		priority = 1

		trigger = {
			scope:actor ?= {
				capital ?= {
					OR = {
						is_in_geographic_region = geographic_region_north_india
						is_in_geographic_region = geographic_region_south_india
					}
				}
				is_country_type = colonial
				is_subject = yes
			}
		}
	}
}
INJECT:DEFAULT = { #Results in names like "French China" for a colonial subject in china (maybe wanna do company later)
	dynamic_country_name = {
		name = CHI
		adjective = CHI_ADJ
		use_overlord_prefix = yes
		
		priority = 1

		trigger = {
			scope:actor ?= {
				capital ?= {
					OR = {
						is_in_geographic_region = geographic_region_north_china
						is_in_geographic_region = geographic_region_south_china
					}
				}
				is_country_type = colonial
				is_subject = yes
			}
		}
	}
}
INJECT:DEFAULT = { #[currently unused] Results in names like "French East Indies" for a colonial subject in indonesia (maybe wanna do company later)
	dynamic_country_name = {
		name = dyn_c_east_indies
		adjective = dyn_c_east_indies_adj
		use_overlord_prefix = yes
		
		priority = 1

		trigger = {
			scope:actor ?= {
				capital ?= {
					OR = {
						is_in_geographic_region = geographic_region_indonesia
					}
				}
				is_country_type = colonial
				is_subject = yes
			}
		}
	}
}