# For determining if the rename state button should be shown
HMPS_scripted_gui_allow_players_to_rename_states = { ### Search for this scripted GUIs name in "state_panel.gui" to find how it works
    scope = state
    saved_scopes = { frame } #idk if this line is needed

    is_shown = {
        has_game_rule = HMPS_game_rule_allow_players_to_rename_states_enabled
    }
	
    is_valid = { #idk if this "is_valid" part is needed
        has_game_rule = HMPS_game_rule_allow_players_to_rename_states_enabled
    }
}

hmps_afro_american_states_sgui = {
    scope = country

    effect = {
        if = {
            limit = {
                has_variable_list = hmps_afroamerican_states
            }
            every_in_list = {
                variable = hmps_afroamerican_states

                custom_tooltip = "ACW_AFRO_AMERICAN_STATES_LIST_ENTRY" # for each print a line
            }
        }
        # no empty state here
    }
}
