trade_partners_total_imports_from_target = {
	value = "market_imports(scope:base_market)"
}

trade_partners_total_exports_to_target = {
	value = "market_exports(scope:base_market)"
}

trade_partners_total_trade = {
	add = "market_imports(scope:base_market)"
	add = "market_exports(scope:base_market)"
}
