je_ottomanism_mashriqi_button = {
	name = "je_ottomanism_mashriqi_button"
	desc = "je_ottomanism_mashriqi_button_desc"

	visible = {
		always = yes
	}

	selected = {
		cu:mashriqi = {
			has_discrimination_trait = tradition_rumelian
		}
	}

	ai_chance = {
		value = 10
	}

	possible = {
		custom_tooltip = { text = ottomanism_started_tt has_variable = ottomanism_started }
		custom_tooltip = { text = danubian_federation_button_cooldown_tt NOT = { has_variable = danubian_federation_button_cooldown } }
		hidden_trigger = { cu:mashriqi = { custom_tooltip = { text = already_integrated_tt   ROOT = { NOT = { any_in_list = { variable = tur_integrated_cultures cu:mashriqi ?= this } } } } } }
		OR = {
			average_sol_for_culture = { target = cu:mashriqi value >= var:integrated_cultures_sol_requirement }
			any_subject_or_below = {
				average_sol_for_culture = { target = cu:mashriqi value >= prev.var:integrated_cultures_sol_requirement }
			}
		}
	}

	effect = {
		cu:mashriqi = {
			save_scope_as = temp_culture
			add_tradition_trait = tradition_rumelian
		}
		set_variable = { name = danubian_federation_button_cooldown days = 180 } # Setting Cool Down, if you change the duration of this make sure to change the loc (danubian_federation_button_cooldown_tt) for it as well
		add_to_variable_list = { name = aus_integrated_cultures target = scope:temp_culture }
		change_variable = { name = count_integrated_cultures add = 1 }
		change_variable = { name = integrated_cultures_sol_requirement add = var:integrated_cultures_sol_requirement_increase_per_culture }
		scope:temp_culture = { custom_tooltip = incorporate_any_this_culture_states_including_in_ai_subjects_tt }
		hidden_effect = { ### Annexing any homeland states from any AI subjects, Then incorporating all homeland states in austria and all subjects
			every_subject_or_below = {
				every_scope_state = {
					limit = { state_region = { is_homeland = scope:temp_culture } }
					if = { limit = { owner = { is_player = no } }
						set_state_owner = root
					}
					set_state_type = incorporated
				}
			}
			every_scope_state = {
				limit = { state_region = { is_homeland = scope:temp_culture } is_incorporated = no }
				set_state_type = incorporated
			}
		}
        add_radicals = { culture = cu:turkish value = 0.05 }
        add_loyalists = { culture = scope:temp_culture value = 0.05 }
		every_subject_or_below = { ### giving subjects the radicals and loyalists as well
			add_radicals = { culture = cu:turkish value = 0.05 }
			add_loyalists = { culture = scope:temp_culture value = 0.05 }
		}
	}

	ai_chance = {
		value = 100
	}
}

je_ottomanism_bulgarian_button = {
	name = "je_ottomanism_bulgarian_button"
	desc = "je_ottomanism_bulgarian_button_desc"

	visible = {
		always = yes
	}

	selected = {
		cu:bulgarian = {
			has_discrimination_trait = tradition_rumelian
		}
	}

	ai_chance = {
		value = 10
	}

	possible = {
		custom_tooltip = { text = ottomanism_started_tt has_variable = ottomanism_started }
		custom_tooltip = { text = danubian_federation_button_cooldown_tt NOT = { has_variable = danubian_federation_button_cooldown } }
		hidden_trigger = { cu:bulgarian = { custom_tooltip = { text = already_integrated_tt   ROOT = { NOT = { any_in_list = { variable = tur_integrated_cultures cu:bulgarian ?= this } } } } } }
		OR = {
			average_sol_for_culture = { target = cu:bulgarian value >= var:integrated_cultures_sol_requirement }
			any_subject_or_below = {
				average_sol_for_culture = { target = cu:bulgarian value >= prev.var:integrated_cultures_sol_requirement }
			}
		}
	}

	effect = {
		cu:bulgarian = {
			save_scope_as = temp_culture
			add_tradition_trait = tradition_rumelian
		}
		set_variable = { name = danubian_federation_button_cooldown days = 180 } # Setting Cool Down, if you change the duration of this make sure to change the loc (danubian_federation_button_cooldown_tt) for it as well
		add_to_variable_list = { name = aus_integrated_cultures target = scope:temp_culture }
		change_variable = { name = count_integrated_cultures add = 1 }
		change_variable = { name = integrated_cultures_sol_requirement add = var:integrated_cultures_sol_requirement_increase_per_culture }
		scope:temp_culture = { custom_tooltip = incorporate_any_this_culture_states_including_in_ai_subjects_tt }
		hidden_effect = { ### Annexing any homeland states from any AI subjects, Then incorporating all homeland states in austria and all subjects
			every_subject_or_below = {
				every_scope_state = {
					limit = { state_region = { is_homeland = scope:temp_culture } }
					if = { limit = { owner = { is_player = no } }
						set_state_owner = root
					}
					set_state_type = incorporated
				}
			}
			every_scope_state = {
				limit = { state_region = { is_homeland = scope:temp_culture } is_incorporated = no }
				set_state_type = incorporated
			}
		}
        add_radicals = { culture = cu:turkish value = 0.05 }
        add_loyalists = { culture = scope:temp_culture value = 0.05 }
		every_subject_or_below = { ### giving subjects the radicals and loyalists as well
			add_radicals = { culture = cu:turkish value = 0.05 }
			add_loyalists = { culture = scope:temp_culture value = 0.05 }
		}
	}

	ai_chance = {
		value = 100
	}
}

je_ottomanism_kurdish_button = {
	name = "je_ottomanism_kurdish_button"
	desc = "je_ottomanism_kurdish_button_desc"

	visible = {
		always = yes
	}

	selected = {
		cu:kurdish = {
			has_discrimination_trait = tradition_rumelian
		}
	}

	ai_chance = {
		value = 10
	}

	possible = {
		custom_tooltip = { text = ottomanism_started_tt has_variable = ottomanism_started }
		custom_tooltip = { text = danubian_federation_button_cooldown_tt NOT = { has_variable = danubian_federation_button_cooldown } }
		hidden_trigger = { cu:kurdish = { custom_tooltip = { text = already_integrated_tt   ROOT = { NOT = { any_in_list = { variable = tur_integrated_cultures cu:kurdish ?= this } } } } } }
		OR = {
			average_sol_for_culture = { target = cu:kurdish value >= var:integrated_cultures_sol_requirement }
			any_subject_or_below = {
				average_sol_for_culture = { target = cu:kurdish value >= prev.var:integrated_cultures_sol_requirement }
			}
		}
	}

	effect = {
		cu:kurdish = {
			save_scope_as = temp_culture
			add_tradition_trait = tradition_rumelian
		}
		set_variable = { name = danubian_federation_button_cooldown days = 180 } # Setting Cool Down, if you change the duration of this make sure to change the loc (danubian_federation_button_cooldown_tt) for it as well
		add_to_variable_list = { name = aus_integrated_cultures target = scope:temp_culture }
		change_variable = { name = count_integrated_cultures add = 1 }
		change_variable = { name = integrated_cultures_sol_requirement add = var:integrated_cultures_sol_requirement_increase_per_culture }
		scope:temp_culture = { custom_tooltip = incorporate_any_this_culture_states_including_in_ai_subjects_tt }
		hidden_effect = { ### Annexing any homeland states from any AI subjects, Then incorporating all homeland states in austria and all subjects
			every_subject_or_below = {
				every_scope_state = {
					limit = { state_region = { is_homeland = scope:temp_culture } }
					if = { limit = { owner = { is_player = no } }
						set_state_owner = root
					}
					set_state_type = incorporated
				}
			}
			every_scope_state = {
				limit = { state_region = { is_homeland = scope:temp_culture } is_incorporated = no }
				set_state_type = incorporated
			}
		}
        add_radicals = { culture = cu:turkish value = 0.05 }
        add_loyalists = { culture = scope:temp_culture value = 0.05 }
		every_subject_or_below = { ### giving subjects the radicals and loyalists as well
			add_radicals = { culture = cu:turkish value = 0.05 }
			add_loyalists = { culture = scope:temp_culture value = 0.05 }
		}
	}

	ai_chance = {
		value = 100
	}
}

je_ottomanism_armenian_button = {
	name = "je_ottomanism_armenian_button"
	desc = "je_ottomanism_armenian_button_desc"

	visible = {
		always = yes
	}

	selected = {
		cu:armenian = {
			has_discrimination_trait = tradition_rumelian
		}
	}

	ai_chance = {
		value = 10
	}

	possible = {
		custom_tooltip = { text = ottomanism_started_tt has_variable = ottomanism_started }
		custom_tooltip = { text = danubian_federation_button_cooldown_tt NOT = { has_variable = danubian_federation_button_cooldown } }
		hidden_trigger = { cu:armenian = { custom_tooltip = { text = already_integrated_tt   ROOT = { NOT = { any_in_list = { variable = tur_integrated_cultures cu:armenian ?= this } } } } } }
		OR = {
			average_sol_for_culture = { target = cu:armenian value >= var:integrated_cultures_sol_requirement }
			any_subject_or_below = {
				average_sol_for_culture = { target = cu:armenian value >= prev.var:integrated_cultures_sol_requirement }
			}
		}
	}

	effect = {
		cu:armenian = {
			save_scope_as = temp_culture
			add_tradition_trait = tradition_rumelian
		}
		set_variable = { name = danubian_federation_button_cooldown days = 180 } # Setting Cool Down, if you change the duration of this make sure to change the loc (danubian_federation_button_cooldown_tt) for it as well
		add_to_variable_list = { name = aus_integrated_cultures target = scope:temp_culture }
		change_variable = { name = count_integrated_cultures add = 1 }
		change_variable = { name = integrated_cultures_sol_requirement add = var:integrated_cultures_sol_requirement_increase_per_culture }
		scope:temp_culture = { custom_tooltip = incorporate_any_this_culture_states_including_in_ai_subjects_tt }
		hidden_effect = { ### Annexing any homeland states from any AI subjects, Then incorporating all homeland states in austria and all subjects
			every_subject_or_below = {
				every_scope_state = {
					limit = { state_region = { is_homeland = scope:temp_culture } }
					if = { limit = { owner = { is_player = no } }
						set_state_owner = root
					}
					set_state_type = incorporated
				}
			}
			every_scope_state = {
				limit = { state_region = { is_homeland = scope:temp_culture } is_incorporated = no }
				set_state_type = incorporated
			}
		}
        add_radicals = { culture = cu:turkish value = 0.05 }
        add_loyalists = { culture = scope:temp_culture value = 0.05 }
		every_subject_or_below = { ### giving subjects the radicals and loyalists as well
			add_radicals = { culture = cu:turkish value = 0.05 }
			add_loyalists = { culture = scope:temp_culture value = 0.05 }
		}
	}

	ai_chance = {
		value = 100
	}
}

je_ottomanism_greek_button = {
	name = "je_ottomanism_greek_button"
	desc = "je_ottomanism_greek_button_desc"

	visible = {
		always = yes
	}

	selected = {
		cu:greek = {
			has_discrimination_trait = tradition_rumelian
		}
	}

	ai_chance = {
		value = 10
	}

	possible = {
		custom_tooltip = { text = ottomanism_started_tt has_variable = ottomanism_started }
		custom_tooltip = { text = danubian_federation_button_cooldown_tt NOT = { has_variable = danubian_federation_button_cooldown } }
		hidden_trigger = { cu:greek = { custom_tooltip = { text = already_integrated_tt   ROOT = { NOT = { any_in_list = { variable = tur_integrated_cultures cu:greek ?= this } } } } } }
		OR = {
			average_sol_for_culture = { target = cu:greek value >= var:integrated_cultures_sol_requirement }
			any_subject_or_below = {
				average_sol_for_culture = { target = cu:greek value >= prev.var:integrated_cultures_sol_requirement }
			}
		}
	}

	effect = {
		cu:greek = {
			save_scope_as = temp_culture
			add_tradition_trait = tradition_rumelian
		}
		set_variable = { name = danubian_federation_button_cooldown days = 180 } # Setting Cool Down, if you change the duration of this make sure to change the loc (danubian_federation_button_cooldown_tt) for it as well
		add_to_variable_list = { name = aus_integrated_cultures target = scope:temp_culture }
		change_variable = { name = count_integrated_cultures add = 1 }
		change_variable = { name = integrated_cultures_sol_requirement add = var:integrated_cultures_sol_requirement_increase_per_culture }
		scope:temp_culture = { custom_tooltip = incorporate_any_this_culture_states_including_in_ai_subjects_tt }
		hidden_effect = { ### Annexing any homeland states from any AI subjects, Then incorporating all homeland states in austria and all subjects
			every_subject_or_below = {
				every_scope_state = {
					limit = { state_region = { is_homeland = scope:temp_culture } }
					if = { limit = { owner = { is_player = no } }
						set_state_owner = root
					}
					set_state_type = incorporated
				}
			}
			every_scope_state = {
				limit = { state_region = { is_homeland = scope:temp_culture } is_incorporated = no }
				set_state_type = incorporated
			}
		}
        add_radicals = { culture = cu:turkish value = 0.05 }
        add_loyalists = { culture = scope:temp_culture value = 0.05 }
		every_subject_or_below = { ### giving subjects the radicals and loyalists as well
			add_radicals = { culture = cu:turkish value = 0.05 }
			add_loyalists = { culture = scope:temp_culture value = 0.05 }
		}
	}

	ai_chance = {
		value = 100
	}
}

je_ottomanism_serb_button = {
	name = "je_ottomanism_serb_button"
	desc = "je_ottomanism_serb_button_desc"

	visible = {
		always = yes
	}

	selected = {
		cu:serb = {
			has_discrimination_trait = tradition_rumelian
		}
	}

	ai_chance = {
		value = 10
	}

	possible = {
		custom_tooltip = { text = ottomanism_started_tt has_variable = ottomanism_started }
		custom_tooltip = { text = danubian_federation_button_cooldown_tt NOT = { has_variable = danubian_federation_button_cooldown } }
		hidden_trigger = { cu:serb = { custom_tooltip = { text = already_integrated_tt   ROOT = { NOT = { any_in_list = { variable = tur_integrated_cultures cu:serb ?= this } } } } } }
		OR = {
			average_sol_for_culture = { target = cu:serb value >= var:integrated_cultures_sol_requirement }
			any_subject_or_below = {
				average_sol_for_culture = { target = cu:serb value >= prev.var:integrated_cultures_sol_requirement }
			}
		}
	}

	effect = {
		cu:serb = {
			save_scope_as = temp_culture
			add_tradition_trait = tradition_rumelian
		}
		set_variable = { name = danubian_federation_button_cooldown days = 180 } # Setting Cool Down, if you change the duration of this make sure to change the loc (danubian_federation_button_cooldown_tt) for it as well
		add_to_variable_list = { name = aus_integrated_cultures target = scope:temp_culture }
		change_variable = { name = count_integrated_cultures add = 1 }
		change_variable = { name = integrated_cultures_sol_requirement add = var:integrated_cultures_sol_requirement_increase_per_culture }
		scope:temp_culture = { custom_tooltip = incorporate_any_this_culture_states_including_in_ai_subjects_tt }
		hidden_effect = { ### Annexing any homeland states from any AI subjects, Then incorporating all homeland states in austria and all subjects
			every_subject_or_below = {
				every_scope_state = {
					limit = { state_region = { is_homeland = scope:temp_culture } }
					if = { limit = { owner = { is_player = no } }
						set_state_owner = root
					}
					set_state_type = incorporated
				}
			}
			every_scope_state = {
				limit = { state_region = { is_homeland = scope:temp_culture } is_incorporated = no }
				set_state_type = incorporated
			}
		}
        add_radicals = { culture = cu:turkish value = 0.05 }
        add_loyalists = { culture = scope:temp_culture value = 0.05 }
		every_subject_or_below = { ### giving subjects the radicals and loyalists as well
			add_radicals = { culture = cu:turkish value = 0.05 }
			add_loyalists = { culture = scope:temp_culture value = 0.05 }
		}
	}

	ai_chance = {
		value = 100
	}
}

je_ottomanism_romanian_button = {
	name = "je_ottomanism_romanian_button"
	desc = "je_ottomanism_romanian_button_desc"

	visible = {
		always = yes
	}

	selected = {
		cu:romanian = {
			has_discrimination_trait = tradition_rumelian
		}
	}

	ai_chance = {
		value = 10
	}

	possible = {
		custom_tooltip = { text = ottomanism_started_tt has_variable = ottomanism_started }
		custom_tooltip = { text = danubian_federation_button_cooldown_tt NOT = { has_variable = danubian_federation_button_cooldown } }
		hidden_trigger = { cu:romanian = { custom_tooltip = { text = already_integrated_tt   ROOT = { NOT = { any_in_list = { variable = tur_integrated_cultures cu:romanian ?= this } } } } } }
		OR = {
			average_sol_for_culture = { target = cu:romanian value >= var:integrated_cultures_sol_requirement }
			any_subject_or_below = {
				average_sol_for_culture = { target = cu:romanian value >= prev.var:integrated_cultures_sol_requirement }
			}
		}
	}

	effect = {
		cu:romanian = {
			save_scope_as = temp_culture
			add_tradition_trait = tradition_rumelian
		}
		set_variable = { name = danubian_federation_button_cooldown days = 180 } # Setting Cool Down, if you change the duration of this make sure to change the loc (danubian_federation_button_cooldown_tt) for it as well
		add_to_variable_list = { name = aus_integrated_cultures target = scope:temp_culture }
		change_variable = { name = count_integrated_cultures add = 1 }
		change_variable = { name = integrated_cultures_sol_requirement add = var:integrated_cultures_sol_requirement_increase_per_culture }
		scope:temp_culture = { custom_tooltip = incorporate_any_this_culture_states_including_in_ai_subjects_tt }
		hidden_effect = { ### Annexing any homeland states from any AI subjects, Then incorporating all homeland states in austria and all subjects
			every_subject_or_below = {
				every_scope_state = {
					limit = { state_region = { is_homeland = scope:temp_culture } }
					if = { limit = { owner = { is_player = no } }
						set_state_owner = root
					}
					set_state_type = incorporated
				}
			}
			every_scope_state = {
				limit = { state_region = { is_homeland = scope:temp_culture } is_incorporated = no }
				set_state_type = incorporated
			}
		}
        add_radicals = { culture = cu:turkish value = 0.05 }
        add_loyalists = { culture = scope:temp_culture value = 0.05 }
		every_subject_or_below = { ### giving subjects the radicals and loyalists as well
			add_radicals = { culture = cu:turkish value = 0.05 }
			add_loyalists = { culture = scope:temp_culture value = 0.05 }
		}
	}

	ai_chance = {
		value = 100
	}
}

je_ottomanism_bedouin_button = {
	name = "je_ottomanism_bedouin_button"
	desc = "je_ottomanism_bedouin_button_desc"

	visible = {
		always = yes
	}

	selected = {
		cu:bedouin = {
			has_discrimination_trait = tradition_rumelian
		}
	}

	ai_chance = {
		value = 10
	}

	possible = {
		custom_tooltip = { text = ottomanism_started_tt has_variable = ottomanism_started }
		custom_tooltip = { text = danubian_federation_button_cooldown_tt NOT = { has_variable = danubian_federation_button_cooldown } }
		hidden_trigger = { cu:bedouin = { custom_tooltip = { text = already_integrated_tt   ROOT = { NOT = { any_in_list = { variable = tur_integrated_cultures cu:bedouin ?= this } } } } } }
		OR = {
			average_sol_for_culture = { target = cu:bedouin value >= var:integrated_cultures_sol_requirement }
			any_subject_or_below = {
				average_sol_for_culture = { target = cu:bedouin value >= prev.var:integrated_cultures_sol_requirement }
			}
		}
	}

	effect = {
		cu:bedouin = {
			save_scope_as = temp_culture
			add_tradition_trait = tradition_rumelian
		}
		set_variable = { name = danubian_federation_button_cooldown days = 180 } # Setting Cool Down, if you change the duration of this make sure to change the loc (danubian_federation_button_cooldown_tt) for it as well
		add_to_variable_list = { name = aus_integrated_cultures target = scope:temp_culture }
		change_variable = { name = count_integrated_cultures add = 1 }
		change_variable = { name = integrated_cultures_sol_requirement add = var:integrated_cultures_sol_requirement_increase_per_culture }
		scope:temp_culture = { custom_tooltip = incorporate_any_this_culture_states_including_in_ai_subjects_tt }
		hidden_effect = { ### Annexing any homeland states from any AI subjects, Then incorporating all homeland states in austria and all subjects
			every_subject_or_below = {
				every_scope_state = {
					limit = { state_region = { is_homeland = scope:temp_culture } }
					if = { limit = { owner = { is_player = no } }
						set_state_owner = root
					}
					set_state_type = incorporated
				}
			}
			every_scope_state = {
				limit = { state_region = { is_homeland = scope:temp_culture } is_incorporated = no }
				set_state_type = incorporated
			}
		}
        add_radicals = { culture = cu:turkish value = 0.05 }
        add_loyalists = { culture = scope:temp_culture value = 0.05 }
		every_subject_or_below = { ### giving subjects the radicals and loyalists as well
			add_radicals = { culture = cu:turkish value = 0.05 }
			add_loyalists = { culture = scope:temp_culture value = 0.05 }
		}
	}

	ai_chance = {
		value = 100
	}
}

je_ottomanism_maghrebi_button = {
	name = "je_ottomanism_maghrebi_button"
	desc = "je_ottomanism_maghrebi_button_desc"

	visible = {
		always = yes
	}

	selected = {
		cu:maghrebi = {
			has_discrimination_trait = tradition_rumelian
		}
	}

	ai_chance = {
		value = 10
	}

	possible = {
		custom_tooltip = { text = ottomanism_started_tt has_variable = ottomanism_started }
		custom_tooltip = { text = danubian_federation_button_cooldown_tt NOT = { has_variable = danubian_federation_button_cooldown } }
		hidden_trigger = { cu:maghrebi = { custom_tooltip = { text = already_integrated_tt   ROOT = { NOT = { any_in_list = { variable = tur_integrated_cultures cu:maghrebi ?= this } } } } } }
		OR = {
			average_sol_for_culture = { target = cu:maghrebi value >= var:integrated_cultures_sol_requirement }
			any_subject_or_below = {
				average_sol_for_culture = { target = cu:maghrebi value >= prev.var:integrated_cultures_sol_requirement }
			}
		}
	}

	effect = {
		cu:maghrebi = {
			save_scope_as = temp_culture
			add_tradition_trait = tradition_rumelian
		}
		set_variable = { name = danubian_federation_button_cooldown days = 180 } # Setting Cool Down, if you change the duration of this make sure to change the loc (danubian_federation_button_cooldown_tt) for it as well
		add_to_variable_list = { name = aus_integrated_cultures target = scope:temp_culture }
		change_variable = { name = count_integrated_cultures add = 1 }
		change_variable = { name = integrated_cultures_sol_requirement add = var:integrated_cultures_sol_requirement_increase_per_culture }
		scope:temp_culture = { custom_tooltip = incorporate_any_this_culture_states_including_in_ai_subjects_tt }
		hidden_effect = { ### Annexing any homeland states from any AI subjects, Then incorporating all homeland states in austria and all subjects
			every_subject_or_below = {
				every_scope_state = {
					limit = { state_region = { is_homeland = scope:temp_culture } }
					if = { limit = { owner = { is_player = no } }
						set_state_owner = root
					}
					set_state_type = incorporated
				}
			}
			every_scope_state = {
				limit = { state_region = { is_homeland = scope:temp_culture } is_incorporated = no }
				set_state_type = incorporated
			}
		}
        add_radicals = { culture = cu:turkish value = 0.05 }
        add_loyalists = { culture = scope:temp_culture value = 0.05 }
		every_subject_or_below = { ### giving subjects the radicals and loyalists as well
			add_radicals = { culture = cu:turkish value = 0.05 }
			add_loyalists = { culture = scope:temp_culture value = 0.05 }
		}
	}

	ai_chance = {
		value = 100
	}
}

je_ottomanism_misri_button = {
	name = "je_ottomanism_misri_button"
	desc = "je_ottomanism_misri_button_desc"

	visible = {
		always = yes
	}

	selected = {
		cu:misri = {
			has_discrimination_trait = tradition_rumelian
		}
	}

	ai_chance = {
		value = 10
	}

	possible = {
		custom_tooltip = { text = ottomanism_started_tt has_variable = ottomanism_started }
		custom_tooltip = { text = danubian_federation_button_cooldown_tt NOT = { has_variable = danubian_federation_button_cooldown } }
		hidden_trigger = { cu:misri = { custom_tooltip = { text = already_integrated_tt   ROOT = { NOT = { any_in_list = { variable = tur_integrated_cultures cu:misri ?= this } } } } } }
		OR = {
			average_sol_for_culture = { target = cu:misri value >= var:integrated_cultures_sol_requirement }
			any_subject_or_below = {
				average_sol_for_culture = { target = cu:misri value >= prev.var:integrated_cultures_sol_requirement }
			}
		}
	}

	effect = {
		cu:misri = {
			save_scope_as = temp_culture
			add_tradition_trait = tradition_rumelian
		}
		set_variable = { name = danubian_federation_button_cooldown days = 180 } # Setting Cool Down, if you change the duration of this make sure to change the loc (danubian_federation_button_cooldown_tt) for it as well
		add_to_variable_list = { name = aus_integrated_cultures target = scope:temp_culture }
		change_variable = { name = count_integrated_cultures add = 1 }
		change_variable = { name = integrated_cultures_sol_requirement add = var:integrated_cultures_sol_requirement_increase_per_culture }
		scope:temp_culture = { custom_tooltip = incorporate_any_this_culture_states_including_in_ai_subjects_tt }
		hidden_effect = { ### Annexing any homeland states from any AI subjects, Then incorporating all homeland states in austria and all subjects
			every_subject_or_below = {
				every_scope_state = {
					limit = { state_region = { is_homeland = scope:temp_culture } }
					if = { limit = { owner = { is_player = no } }
						set_state_owner = root
					}
					set_state_type = incorporated
				}
			}
			every_scope_state = {
				limit = { state_region = { is_homeland = scope:temp_culture } is_incorporated = no }
				set_state_type = incorporated
			}
		}
        add_radicals = { culture = cu:turkish value = 0.05 }
        add_loyalists = { culture = scope:temp_culture value = 0.05 }
		every_subject_or_below = { ### giving subjects the radicals and loyalists as well
			add_radicals = { culture = cu:turkish value = 0.05 }
			add_loyalists = { culture = scope:temp_culture value = 0.05 }
		}
	}

	ai_chance = {
		value = 100
	}
}

je_ottomanism_enact_the_federation = {
	name = "je_ottomanism_enact_the_federation"
	desc = "je_ottomanism_enact_the_federation_desc"

	visible = {
		always = yes
	}

	ai_chance = {
		value = 10
	}

	possible = {
		trigger_if = {
			limit = {
				has_variable = count_integrated_cultures
			}
			custom_tooltip = {
				text = has_integrated_5_danubian_cultures_tt
				var:count_integrated_cultures >= 5
				NOT = { has_variable = ottomanism_federation_final_act }
			}
		}
	}

	effect = {
		custom_tooltip = {
			text = federalization_signed_tt
			set_variable = ottomanism_federation_final_act
		}
		custom_tooltip = {
			text = ottoman_fed_tt.c
		}
		activate_law = law_type:law_multicultural
		add_loyalists = {
			value = 0.1
		}
	}

	selected = {
		custom_tooltip = {
			text = federalization_was_signed_tt
			has_variable = ottomanism_federation_final_act
		}
	}

	ai_chance = {
		value = 100
	}
}

je_ottomanism_start_button = {
	name = "je_ottomanism_start_button"
	desc = "je_ottomanism_start_button_desc"

	visible = {
		always = yes
	}

	ai_chance = {
		value = 10
	}

	possible = {
		has_technology_researched = pan-nationalism
		OR = {
			has_law_or_variant = law_type:law_total_separation
			#has_law_or_variant = law_type:law_state_atheism # State atheism kinda goes against the idea of being religiously tolerant which is what we are going for here
		}
		has_law_or_variant = law_type:law_elected_bureaucrats
		OR = {
			has_law_or_variant = law_type:law_subjecthood
			has_law_or_variant = law_type:law_multicultural
		}
		OR = {
			has_law_or_variant = law_type:law_census_voting
			has_law_or_variant = law_type:law_universal_suffrage
			has_law_or_variant = law_type:law_anarchy
		}
		hidden_trigger = {
			NOT = { has_variable = ottomanism_started }
		}
	}

	effect = {
		custom_tooltip = {
			text = ottomanism_start_tt
			set_variable = ottomanism_started
		}
		je:je_embrace_ottomanism = {
			add_modifier = modifier_HMPS_embraced_ottomanism
		}
		custom_tooltip = {
			text = incorporate_any_albanian_bosniak_sephardic_states_tt
			every_scope_state = {
				limit = {
					is_incorporated = no
					OR = {
						state_region = { is_homeland = cu:albanian }
						state_region = { is_homeland = cu:bosniak }
						state_region = { is_homeland = cu:sephardic }
					}
				}
				set_state_type = incorporated
			}
		}
	}

	selected = {
		custom_tooltip = {
			text = ottomanism_started_tt
			has_variable = ottomanism_started
		}
	}

	ai_chance = {
		value = 100
	}
}