#
# Note: the triggers are checked at certain key points in the code, not all the time
# color_key = {
#    color = "black"      # the map color
#    possible = { ... }   # the trigger to enable this color
# }
#

# Example
#dynamic_map_color_swedish_junta = {
#    color = "black"
#
#	possible = {
#		exists = c:SWE # you /must/ check that a tag exists before doing anything else
#		c:SWE ?= THIS
#
#        exists = ig:ig_armed_forces
#        ig:ig_armed_forces ?= {
#            is_in_government = yes
#        }
#	}
#}

HMPS_ottoman_federation = { #This is done so that after doing ottomanism, that turkey gets cool colour
	color = "turkish_republic_red"

	possible = {
		exists = c:TUR
		c:TUR ?= THIS
		OR = {
			has_law_or_variant = law_type:law_multicultural
		}
	}
}

REPLACE:united_states_of_greater_austria = { #This way we disable to boring base color of united_states_of_greater_austria
	color = "usga_grey"
	
	possible = {
		always = no
	}
}